#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../multitransport-jsonrpc/bin/jsonrpc-repl" "$@"
  ret=$?
else 
  node  "$basedir/../multitransport-jsonrpc/bin/jsonrpc-repl" "$@"
  ret=$?
fi
exit $ret
